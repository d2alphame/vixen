8 Bytes Address Bus

The default memory map.

These are all built into the fabric and enabled by default
0xFFFFFFFFFFFF0000 ROM 64kb
0xFFFFFFFFFFFEF000 Base Address of fabric registers (4K)
0xFFFFFFFFFFFEE000 Address of UART (4K)
